/** @type {import('next').NextConfig} */

const nextConfig = {
    /*  // Partial Prerendering (PPR) does not work with vercel yet
    experimental: {
        ppr: 'incremental',
    }, 
    */
};

export default nextConfig;